const router = require('express').Router();
const { auth } = require('../middleware/auth');
// You can add endpoints to get donor profile, etc.
// For now, we don't need extra donor-specific routes.

module.exports = router;